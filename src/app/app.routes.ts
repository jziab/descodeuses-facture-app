import { Routes } from '@angular/router';
import { FactureListeComponent } from './facture-liste/facture-liste.component';

export const routes: Routes = [
    {path: 'facture', component: FactureListeComponent, title: 'facture'}
];
